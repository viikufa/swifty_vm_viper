import UIKit

class {{ module_info.name }}Initializer: NSObject {
    @IBOutlet weak var viewController: {{ module_info.name }}ViewController!

    override func awakeFromNib() {
        super.awakeFromNib()
        {{ module_info.name }}Configurator().configure(view: viewController)
    }
}
